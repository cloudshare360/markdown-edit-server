# Copy this file to .env and fill with your Git credentials (local-only)
# This file is NOT used directly by git; use the helper script/command below to write .git-credentials.

GIT_USERNAME=your-username
GIT_PAT=your-token
